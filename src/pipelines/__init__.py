"""Comparison tracks.

Each track is one intervention evaluated under identical conditions: the same split
artifact, the same encoder, the same search space, the same metric code and the same
bootstrap replicates. Only the intervention differs, which is what makes a difference
between two tracks attributable to the intervention rather than to the setup.
"""
