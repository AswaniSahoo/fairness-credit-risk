# Training and evaluation environment. Python >= 3.11.
#
# Core versions are aligned with google-research/tabfm so the TabFM comparison track
# (see .agents/plans/tabfm-integration.md) installs into this same environment.
#
# aif360 is deliberately absent: it cannot coexist with numpy 2.x, and the group
# fairness metrics plus Kamiran-Calders reweighing are implemented in-repo with unit
# tests instead. See decision C5 in the TabFM integration plan.

numpy==2.2.0
pandas==2.2.3
scipy==1.15.1
scikit-learn==1.6.0
joblib==1.4.2

xgboost==2.1.4
lightgbm==4.5.0

optuna==4.1.0

# Group fairness metrics and the ExponentiatedGradient in-processing reduction.
fairlearn==0.12.0

# Per-decision feature attributions for adverse-action reason codes (ECOA / Reg B).
shap==0.46.0

matplotlib==3.10.0
seaborn==0.13.2
